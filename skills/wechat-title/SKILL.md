---
name: wechat-title
description: 公众号爆款标题创作 · 按主题拉近期同赛道爆款，提炼可复用的标题套路并生成候选标题（也可对你给的标题做套路化评判）。当用户需要公众号标题、爆款标题、标题生成、起标题、标题套路、标题优化时使用。触发词：公众号标题、爆款标题、标题生成、起标题、标题套路、标题优化。
---

# 公众号爆款标题创作（都爆鸭）

本鸭帮你按主题拉**近期同赛道爆款**，从一堆真实跑出量的标题里提炼可复用套路（数字钩子、悬念、对立、利益点……），再给你一组**可直接用的候选标题**。也能反过来——你给一个标题，本鸭按套路帮你判它强在哪、弱在哪、怎么改。

> 数据走 **doubaoya.com** 一条线，鉴权用你自己的口令（环境变量 `DOUBAOYA_API_KEY`，形如 `dyh_…`）。

---

## 适用场景

| 场景 | 怎么用 | 拿到什么 |
|------|--------|----------|
| **标题创作** | 给主题，本鸭拉爆款提套路、出候选标题 | 一组可直接用的候选标题 |
| **套路提炼** | 想知道这个题材爆款标题怎么起 | 可复用的标题公式清单 |
| **标题评判** | 给一个标题，让本鸭按套路判强弱 | 优化方向 + 改写版本 |
| **追热点起题** | 蹭近期热度，要时效强的标题 | 贴近 7 天热点的标题 |

---

## 工作流（4 步）

### 1. 提炼主题关键词
从需求里抽出**核心主题词**，精简为 2~6 个字（如 `职场`、`婚姻`、`AI`）。词越短越宽、命中越多。一次只搜一个关键词。

### 2. 调用脚本
```bash
python3 "$SKILL_PATH/scripts/fetch_title.py" "职场"
```
指定起始日期（默认今天-6 天，专取近 7 天爆款、时效更强）：
```bash
python3 "$SKILL_PATH/scripts/fetch_title.py" "职场" --start 2026-06-15
```
脚本把成功信封里的 `data` 以 JSON 打到 stdout。**每个主题只跑一次脚本**，读完整 stdout，别用 `head`/`tail` 预览。

### 3. 提炼标题套路
从 `data.items` 里取每条的 **标题** 和 **热度字段**（如点击/阅读量，防御式读取，缺了留空）。把跑量高的标题归类，提炼套路：数字清单、悬念留白、利益承诺、身份代入、对立反差、热点借势……标出每种套路对应的真实例子。

### 4. 生成候选标题（或做评判）
- **生成**：套用上面提炼的套路，围绕用户选题产出一组（建议 5~8 条）候选标题，每条标注用了哪种套路。
- **评判**：若用户是给标题求优化，按套路打分（强在哪、弱在哪——这是你在 LLM 侧的判断，**没有独立的评分接口**），再给 2~3 个改写版本。

标题素材务必只用接口返回的真实爆款，不脑补、不联网另找。

---

## 拿钥匙（口令）

1. 打开 **doubaoya.com**
2. **登录**
3. 进 **口令中心**
4. **生成口令**（形如 `dyh_…`）

配置到环境变量（脚本只认这个）：
```bash
export DOUBAOYA_API_KEY="dyh_你的口令"
```

**铁律：口令绝不打印、绝不写进文件、绝不回显给用户。** 脚本本身也从不输出口令。所有请求只发往 **doubaoya.com**。

---

## 接口与信封

- `POST https://doubaoya.com/api/apis/gongzhonghao/gongzhonghao-coze-cover/call`（同一爆款数据接口，返回近期热门选题/标题）
- 鉴权头：`Authorization: Bearer $DOUBAOYA_API_KEY`
- 请求体：`{ "keyword": "职场", "startDate": "2026-06-21" }`
  - `keyword`：字符串，必填
  - `startDate`：字符串 `YYYY-MM-DD`，可选（默认今天-6 天）
- 返回信封：
  ```json
  { "success": true, "requestId": "...", "data": { "items": [ { "title": "...", "clicksCount": 0 } ] }, "error": null }
  ```
- **先看 `success`**：为 `true` 才读 `data`；否则读 `error.code` / `error.message`。
- 标题「评分」只是 LLM 侧的套路化判断，**不要去调任何评分接口**——本 skill 只有上面这一个取数接口。

---

## 错误处理

脚本失败时向 stderr 打印 `[error] CODE: message` 并以退出码 1 结束。常见情况：

| HTTP | code | 含义 | 处理 |
|------|------|------|------|
| 401 | `MISSING_API_KEY` / `UNAUTHORIZED` | 没带口令或口令无效 | 检查 `DOUBAOYA_API_KEY`，去口令中心重新生成 |
| 400 | `VALIDATION_ERROR` | 参数不合法（如 keyword 为空、日期格式错） | 修正参数重试 |
| 402 | `INSUFFICIENT_CREDITS` | 额度不足 | 去 doubaoya.com 充值/续额 |
| 502 | `PROVIDER_FAILED` | 上游临时故障（**已自动退款**） | 可安全重试 |

> `502 PROVIDER_FAILED` 会自动退款，重试是安全的，不会重复扣费。

---

## 目录结构

```
wechat-title/
├── SKILL.md                  # 本文件
└── scripts/
    └── fetch_title.py        # 零依赖脚本（urllib），调用 doubaoya.com
```
