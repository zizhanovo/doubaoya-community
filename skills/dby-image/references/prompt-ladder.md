# 出一张图的提示词阶梯

**这是提示词的入口。绝大多数请求读完这一份就够了，不用再读别的。**

核心原则来自 OpenAI 官方 gpt-image 提示词指南，一句话：
**先跑最小基线，再一次只加一个维度**——别一上来就把所有维度堆满。
堆满的提示词不会更好，只会让你不知道是哪一句起了作用。

---

## L0 · 最小基线（先跑这个）

**主体 + 媒介**，一句话。就这样先出一张，看方向对不对。

```
一只戴耳机的卡通鸭子在写稿，扁平插画风
```

用在：用户没给细节、或者你也不确定他要什么。
**出一张比问五个问题快**——一张图能让用户立刻说出「不是这种」，而问题问不出来。

> 💰 每次出图都计费。所以 L0 不是「随便糊一张」，是**用一张最省的图换一次方向确认**。
> 用户已经说清楚要什么了，就直接跳到 L1。

---

## L1 · 三件必需（大多数成品图停在这一级）

在 L0 上加**且只加**这三样：

```
比例：宽幅横版，16:9 比例
主体：一只戴耳机的卡通鸭子坐在书桌前写稿，侧面视角，扁平插画风
排除：不要文字、不要水印、不要边框
```

### 为什么是这三样

| | 为什么必需 |
|---|---|
| **比例** | `size` 参数**完全无效**（正文红线六，七个用例实测）。比例的唯一控制手段就是这一行。不写就是 1254×1254 正方形 |
| **排除项** | 模型最爱自作主张加的是**伪文字**和**装饰边框**。不排除就得返工 |
| **用途**（可选但强烈建议） | OpenAI 指南明说：写明用途（海报 / UI 稿 / 信息图）能让模型自己切换到对的精细度档位 |

### 写法顺序

OpenAI 指南建议的顺序是：**场景/背景 → 主体 → 关键细节 → 约束**。
复杂请求**分行写、每行一个标签**，比堆成一大段更可控——这正是上面那个骨架的形状。

---

## L2 · 只补出问题的那一个维度

出图之后不满意，**先定位不满意在哪一点**，只补那一个维度。

### 症状 → 补哪个维度

| 你看到的问题 | 补这个维度 | 去哪查写法 |
|---|---|---|
| 画面平、没层次、光很假 | **光线** | [`axes.md`](axes.md#光线) |
| 主体位置不对 / 太满 / 没地方放标题 | **构图** | [`axes.md`](axes.md#构图) |
| 颜色和品牌对不上 | **色彩** | [`axes.md`](axes.md#色彩) |
| 图里的字是错的 / 想加标题 | **文字** | [`axes.md`](axes.md#文字) |
| 看起来太"AI"、太干净、不真实 | **质感** | [`axes.md`](axes.md#质感) |
| 要写实照片感但出来像插画 | **相机语言** | [`axes.md`](axes.md#相机语言) |

🔴 **一次只加一个。** 同时加三个维度出来的图如果还是不对，你无法知道是哪一个没起作用，
下一轮只能全推倒重来——而每一轮都要花钱。

⚠️ **别加"质量咒语"。** `8K`、`ultra-detailed`、`masterpiece` 这类词对 gpt-image 帮助很小。
OpenAI 指南明确指出：要写实感，**相机与光线的具体描述比这些形容词可靠得多**。

---

## L3 · 场景骨架（做公众号的四类图时直接抄）

封面 / 文章插图 / IP 主视觉 / 信息图，各有一个填空骨架，
含裁切安全区和「什么时候不该用生图」：[`scenes-wechat.md`](scenes-wechat.md)

---

## L4 · 改图（已经有一张图，要动它）

改图有一套单独的纪律（只改什么、保留什么、怎么防止越改越漂）：[`editing.md`](editing.md)

---

## 特殊入口：用户给了参考图说「我要这种感觉」

**先反推，再出图。这一步不调接口、不花钱**——用你自己的视觉能力看图，
按下面七项逐条写出来，跟用户确认之后再进 L1。

### 反推七项

1. **主体**：画面里最重要的是什么，什么姿态、什么视角
2. **情境**：它在哪儿，周围有什么
3. **媒介 / 风格**：照片 / 扁平插画 / 水彩 / 3D 渲染 / 线稿 / 水墨
4. **色彩**：主色、辅色、饱和度高低、冷调暖调
5. **构图**：主体在什么位置，留白在哪，是否对称
6. **光线**：光从哪来，硬光柔光，阴影明显与否
7. **质感**：颗粒、噪点、笔触、纸纹

### 两条纪律

- **只描述你真的看见的东西。** 看不出用什么模型画的就别写「Midjourney 风格」——
  那是猜的，对出图没有帮助。
- **反推结果先给用户确认。** 你看到的重点和他想要的重点常常不是一回事，
  确认一次比出错三次便宜。

---

## 复现与系列：骨架 + 变量

要出一组风格统一的图（一个号的固定视觉语言），把不变的写死、变的留成变量：

```
比例：{{比例}}
主体：{{主体}}
情境：{{情境}}
风格：扁平插画，粗描边，轻微颗粒质感
色彩：主色 {{品牌主色}}，辅色米黄与深棕
构图：主体居中偏左，右侧留白
排除：不要水印、不要边框
```

每张只填两三个变量。**这比每次重写整段提示词稳定得多**，
也是「让一个号的图看起来是同一个号的」最省力的办法。

🔴 `{{品牌主色}}` 这类**必须从 IP 档案取真值**（正文红线四）。
档案里没有就问用户，别自己填一个。
